<form action="{{ path('BtaskDashboardBundle_item_add') }}" method="post" {{ form_enctype(form) }}>
	{{ form_errors(form) }}

    {{ form_errors(form.subject) }}
    {{ form_widget(form.subject) }}
	{{ form_rest(form) }}
	<input type="submit" value="{{ 'dashboard.postit.save'|trans }}" />
</form>
